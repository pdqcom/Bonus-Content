This Script was submitted by mithandirsghost as a work around for not being allowed to force a reboot. This script minimizes all windows and asks users if they would like to reboot. If running with PDQ, make sure it runs as logged on user so the message box can pop up for them to see,
